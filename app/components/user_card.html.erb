<div id="<%= id %>" class="bg-white border border-gray-200 rounded-lg p-4 shadow-sm">
  <%= helpers.turbo_stream_from broadcast_channel %>

  <div class="text-lg font-bold text-gray-900"><%= @user.name %></div>
  <div class="text-sm text-gray-500 mb-3"><%= @user.email %></div>

  <%= sending_email_spinner %>
  <%= send_button %>
  <%= status_badge %>
</div>
